{
  "name": "markdown-search",
  "displayName": "Markdown Full Text Search",
  "description": "Markdown Full Text Search is a Visual Studio Code extension designed for efficient text searching within Markdown files. This extension allows users to effortlessly locate specific text and generate links to it.",
  "type": "module",
  "keywords": [
    "markdown",
    "search"
  ],
  "publisher": "AlexandriteSoftware",
  "version": "0.2.6",
  "license": "MIT",
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/main.cjs",
  "repository": {
    "type": "git",
    "url": "https://github.com/AlexandriteSoftware/markdown-search.git"
  },
  "bugs": {
    "url": "https://github.com/AlexandriteSoftware/markdown-search/issues"
  },
  "contributes": {
    "commands": [
      {
        "command": "markdown-search.search",
        "title": "Markdown Search: Search in Markdown files"
      },
      {
        "command": "markdown-search.add-or-replace-link",
        "title": "Markdown Search: Add link from search results"
      }
    ],
    "configuration": {
      "title": "Markdown Full Text Search",
      "properties": {
        "markdown-search.logging.level": {
          "type": "string",
          "default": "info",
          "enum": [
            "debug",
            "info",
            "notice",
            "warning",
            "error",
            "crit",
            "alert",
            "emerg",
            "none"
          ],
          "scope": "window",
          "description": " none, or RFC5424 logging level: debug, info, notice, warning, error, crit, alert, emerg. Requires restart."
        }
      }
    }
  },
  "scripts": {
    "format:json": "prettier --write --no-error-on-unmatched-pattern \"*.json\"",
    "format": "npm run format:json",
    "build": "tsc -p tsconfig.build.json",
    "build:dist": "esbuild ./src/Extension.ts --bundle --outfile=dist/main.cjs --external:vscode --format=cjs --platform=node --sourcemap",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test": "npm run test:unit && npm run test:vscode",
    "test:unit": "node --test --enable-source-maps build/**/*.test.js",
    "test:vscode": "vscode-test",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "dependencies": {
    "glob": "^13.0.6",
    "minimatch": "^10.2.5",
    "minisearch": "^7.2.0",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.1.1",
    "@types/vscode": "^1.125.0",
    "@typescript-eslint/eslint-plugin": "^8.63.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "acorn": "^8.17.0",
    "asljs-tmpdir": "^0.1.10",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "marked": "^18.0.5",
    "mocha": "^11.7.6",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0"
  }
}
